<!DOCTYPE html>
<html>
<head>
  <title>Grifter</title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>


	<% if user_signed_in? %>
		<%= current_user.email %> | <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
	<% else %>
		<%= link_to 'User Login', new_user_session_path %> | <%= link_to 'Create an Account', new_user_registration_path %>
	<% end %>

	<% if user_signed_in? && !current_user.venmo_account %>
		<p class="connect-venmo-error">
			In order to properly use this app, please connect to your Venmo account.
			<%= link_to 'Click to connect to Venmo', new_venmo_account_path %>
		</p>
	<% end %>

	<% flash.each do |name, msg| %>
	  <% if msg.is_a?(String) %>
	    <%= content_tag :div, msg, :class => "flash_#{name}" %>
	  <% end %>
	<% end %>

<%= yield %>

</body>
</html>
